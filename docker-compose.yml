version: '3.6'
services:
 tomcatserver:
   image: srikanthtekula/apache-tomcat-8.3.2:v1
   container_name: tomcatServer1
   ports:
   -  9090:9090
   volumes:
   -  tomcat-data:/usr/local/tomcat/
   -  /var/run/docker.sock:/var/run/docker.sock
volumes:
  tomcat-data:
